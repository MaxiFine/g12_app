specVersion: "v1beta"
serviceId: "g12app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "g12app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
